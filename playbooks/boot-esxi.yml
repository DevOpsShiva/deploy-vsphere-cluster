- hosts: vms
  gather_facts: false
  any_errors_fatal: true
  sudo: false
  connection: local
  user: remote
  vars_files:
   - /home/sotani/deploy-vsphere-cluster/vars/{{ project_name }}.yml
  roles:
    - { role: boot-esxi }
